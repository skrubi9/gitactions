import {
  Component,
} from '@angular/core';

/**
 * Used as base to be able to have different layouts loaded for the application (e.g. rootcomponent)
 * and the embed page which does not need root components loaded sfsfassdf asf
 */
@Component({
  selector: 'app',
  template: '',
})
export class AppComponent {
}
